

<form action="#" method="post" style="width: 100%">

    <div class="form-group">
        <label  class="control-label">Ad</label>
        <input name="name" type="text" class="form-control"  value="{{ user.name }}"required>

    </div>

    <div class="form-group">
        <label  class="control-label">Adres</label>
        <input name="address" type="text" class="form-control"  value="{{ user.address }}"required>

    </div>



    <div class="form-group">
        <label for="" class="control-label">Mail</label>
        <input name="email" type="text" class="form-control"  value="{{ user.email }}"required>
    </div>
    <div class="form-group">
        <label for="" class="control-label">Telefon</label>
        <input name="phone" type="text" class="form-control"  value="{{ user.phone }}"required>
    </div>


    <div class="form-group">
        <label >Yetki</label>
        <input class="form-control" data-style="btn btn-link"  name="roles" value="{{ user.roles|join }}"disabled>
        <label >Durum</label>
        <input class="form-control" data-style="btn btn-link"  name="roles" value="{{ user.status }}"disabled>

        <input type="hidden"name="token"value="{{ csrf_token('user-form') }}">
    </div>
    <button type="submit" class="btn btn-primary "><i class="material-icons"title="Send">Güncelle</i></button>

</form>